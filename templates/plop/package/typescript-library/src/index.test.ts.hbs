import { describe, expect, test } from "bun:test";

import { create{{pascalCase name}}Descriptor } from "./index.js";

describe("create{{pascalCase name}}Descriptor", () => {
  test("returns the package descriptor", () => {
    expect(create{{pascalCase name}}Descriptor()).toEqual({
      name: "{{dashCase name}}",
      packageName: "@repo/{{dashCase name}}"
    });
  });
});
